<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Keeps the background transparent -->

    <!-- Bottom stroke -->
    <stroke
        android:width="3dp"
        android:color="@color/grey"
        android:dashWidth="0dp"
        android:dashGap="0dp"/>
</shape>
